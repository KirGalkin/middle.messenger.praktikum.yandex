{{{image}}}
<!--<p class="avatar-name">{{name}}</p>-->

{{#if editMode}}
    <form id="myUserForm">
        Upload new avatar image: <input id="avatar" type="file" name="avatar" accept="image/*">
        <!--    <button onclick="onSubmit()">Submit</button>-->
        {{{button}}}
    </form>
{{/if}}
